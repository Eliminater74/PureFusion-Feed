# PureFusion Feed — Parity Roadmap

Last updated: 2026-04-16 (Phase 51)

---

## Definition of Done (DoD)

- Feature works on fresh page load.
- Survives infinite scroll and React-driven node injections.
- Master toggle is fully reversible (content restores on OFF).
- Messenger surface remains unaffected unless specifically targeted.
- Roadmap documentation updated with latest status.

---

## Current Focus (DO NOT SKIP)

Always continue from the highest-priority unfinished item. Do not jump ahead.

**Completed phases (most recent first):**

- ✅ Phase 51: Lifecycle + Disabled-State Hardening — Three correctness gaps closed. (1) **`ui-tweaks.js` `destroy()`** (new method): removes the DFM `keydown` capture listener (`document.removeEventListener('keydown', _dfmKeyHandler, true)`), resets `_dfmActive = false`, removes `pf-dfm-active` class from `document.documentElement`, calls `_restoreWrappedLinks()` to reverse all href replacements, calls `_clearAbsoluteTimestampLabels()` to remove all injected date chips, removes the `<style id="purefusion-ui-tweaks">` element. (2) **`predictor.js` `destroy()`** (new method): clears `syncIntervalId` interval, removes `visibilitychange` listener, removes document-level `click` capture listener, cancels `_friendBadgeDebounce` timeout, removes all `.pf-friend-unseen` badge elements from DOM, removes `<style id="pf-predictor-styles">`, resets `_stylesInjected = false`. (3) **`main.js` `_destroy()` destroyable list** expanded: `this.uiTweaks` and `this.predictor` added — both now participate in the existing `mod.destroy()` pattern. (4) **`main.js` `getEffectiveSettings()` disabled-state overrides** expanded: `effective.filters` gains `removeSponsored`, `removeGameInvites`, `hideMemories`, `hideLiveVideoPosts`, `hideShareReposts`, `hidePollPosts`, `removeLargeReactions`; `effective.uiMode` gains `compactMode`, `widescreenMode`, `fontSizeScale: 100`, `distractionFreeMode`, `friendsOnlyMode`, `anonymizerMode`, `hidePostComposer`, `showLinkPreviews`, `fixTimestamps`, `commentSortDefault: 'All Comments'`; `effective.predictions` gains `trueAffinitySort`, `showFriendActivity`, `showBadge`, `dimLowInterest`, `collapseLowInterest`, `highlightHighInterest`, `credibilitySignalsEnabled`; new `effective.sidebar` block sets `enableModuleFilters: false`.
- ✅ Phase 50: v2.0.0 Release — Popup DFM toggle + version bump + changelog. (1) **Popup Distraction-Free toggle**: new `tgl_distractionFree` row added to Quick Filters section in `popup.html`; `elements.distractionFree` mapped in `popup.js`; added to `quickToggles` array (greyed out when master toggle is OFF); initialized from `settings.uiMode.distractionFreeMode`; `handleToggle` saves `settings.uiMode.distractionFreeMode`; `addEventListener('change', handleToggle)` wired. (2) **Version bump 1.9.0 → 2.0.0**: `manifest.json` `version` field + `popup.html` static fallback updated. (3) **CHANGELOG.md**: new v2.0.0 section documenting all phases 45-49 features: Distraction-Free Mode, Game Invite Filter, True-Affinity Sort, Link Destination Reveal, Comment Sort Enforcement, Absolute Timestamp Chips, Friend Activity Feed Insight, Large Reaction Normalization, Session Stats Live Counting; plus Changed section noting DFM popup toggle and the `data-ad-comet-preview` dual-selector fix.
- ✅ Phase 49: Friend Activity Feed Insight (activate `predictions.showFriendActivity`) — Orphaned default-ON setting fully implemented. `PF_Predictor` gains `_sessionFeedAuthors` (Set) and `_friendBadgeDebounce` in constructor. `_processSingleNode`: when `showFriendActivity` is ON, extracts the post author and adds the normalized (lowercase/trimmed) name to `_sessionFeedAuthors` — building a session-level record of which authors appeared in the feed. `sweepDocument()`: after `applyToNodes`, triggers a debounced (800ms) `_applyFriendActivityBadges()` call. New `_applyFriendActivityBadges()` method: locates the contacts panel via locale-aware `aria-label` selectors (EN/ES/DE/IT/NL/SV/NO/DA), queries all `[role="listitem"]` / `[role="row"]` rows, extracts friend name from `[aria-label]` or `span[dir="auto"]`, normalizes to lowercase, checks against `_sessionFeedAuthors` using exact match + first-name prefix match (handles "John" in feed vs "John Smith" in contacts), injects `.pf-friend-unseen` "not in feed" badge (amber pill, `pointer-events:none`, tooltip explaining suppression) when the contact hasn't appeared; only fires when ≥3 authors have been seen in the feed this session (prevents false noise on initial page load); removes existing badges when contact is later seen. CSS: `.pf-friend-unseen` amber pill added to `_injectPredictorStyles`. Options: new `auto-height` toggle row in AI Predictions card (after `trueAffinitySort`) with description explaining algorithmic suppression detection; `opt_pred_showFriendActivity` uiMap entry in `options.js`.
- ✅ Phase 48: True-Affinity Sort Options UI + Distraction-Free Reading Mode — Two orphaned settings fully activated. (1) **`predictions.trueAffinitySort: false`**: Feature was already fully implemented in `predictor.js` (`_applyNativeAffinitySort` applies CSS `flexbox order` to reorder posts by local engagement score — highest-scored posts float to the top without touching React's virtual DOM) but had no options UI. Added `auto-height` toggle row to the AI Predictions card in `options.html` (after `opt_pred_showTrending`, with description `<p>` explaining the CSS flexbox strategy). Added `opt_pred_trueAffinitySort` uiMap entry in `options.js` (`obj: 'predictions', prop: 'trueAffinitySort', type: 'checkbox'`). (2) **`uiMode.distractionFreeMode: false`**: No implementation existed anywhere. Full implementation in `ui-tweaks.js`: two new instance fields (`_dfmActive`, `_dfmKeyHandler`); `_setupDistractionFreeMode()` called from `init()` — reads initial state from `settings.uiMode.distractionFreeMode`, calls `_applyDfmClass()`, and registers a capture-phase `keydown` listener for `Alt+Shift+F` that toggles `_dfmActive` and calls `_applyDfmClass()`; `_applyDfmClass()` adds/removes `pf-dfm-active` class on `document.documentElement`; CSS in `update()` — `html.pf-dfm-active` rules hide `[data-pagelet="LeftRail"]`, `[data-pagelet="RightRail"]`, `[role="complementary"]`, and non-primary `[role="navigation"]` with `display:none !important`, and constrain `[role="main"]` to `max-width: 680px` centered with `margin: auto`; `updateSettings()` syncs `_dfmActive` from the settings push (so toggling the options checkbox takes effect immediately) then calls `_applyDfmClass()`. Options: new `auto-height` toggle row in the UI Tweaks section of `options.html` (before the Theme `<hr>`, after `commentSortDefault`), with description `<p>` noting the Alt+Shift+F shortcut; `opt_uiMode_distractionFreeMode` uiMap entry in `options.js`.
- ✅ Phase 47: Link Destination Reveal + Comment Sort Enforcement — Two orphaned `uiMode` settings activated. (1) **`showLinkPreviews: true`** (default ON, previously did nothing): New `_revealLinkDestinations(nodes)` in `ui-tweaks.js`. Selects all `a[href*="l.facebook.com/l.php"]:not([data-pf-revealed])` and `a[href*="lm.facebook.com/l.php"]:not([data-pf-revealed])` in feed nodes. For each, parses the `u=` query parameter from the tracking URL using `new URL(href).searchParams.get('u')`, validates it starts with `https?://`, stores the original href in `data-pf-original-href`, then replaces `href` with the real destination. This makes the browser status bar and copy-link show the real URL instead of FB's redirect wrapper — direct anti-phishing/tracking-reduction benefit. `data-pf-revealed='1'` marker prevents re-processing. New `_restoreWrappedLinks()` method reverses all replacements on toggle-OFF: restores from `data-pf-original-href`, removes both markers. `update()` calls `_restoreWrappedLinks()` when setting is OFF. `applyDocumentLevelTweaks()` calls `_revealLinkDestinations(null)` for initial page sweep. `applyToNodes(nodes)` processes new batches. (2) **`commentSortDefault: 'All Comments'`** (default = no action; only fires when changed): New `_enforceCommentSort(nodes)` and `_findCommentSortButton(article, sortLabels)` in `ui-tweaks.js`. `_findCommentSortButton` finds `[role="button"][aria-haspopup]` whose full text content matches a known sort label (most relevant / all comments / newest / top comments / most recent) — the exact-text requirement distinguishes the sort button from the Like picker and other aria-haspopup menus. `_enforceCommentSort` maps the setting value to target text patterns (`Newest` → `['newest', 'most recent']`, `Top Comments` → `['most relevant', 'top comments']`), marks the article with `data-pf-sort-enforced` before the async operation to prevent re-entry, then uses a 350ms delay before clicking the sort button + 200ms for the menu to open + click of the first matching `[role="menuitem"]`; newly opened menus are marked `data-pf-menu-handled` to prevent double-click. Only fires for articles without the marker — safe with MutationObserver. Called from `applyToNodes` only when preference ≠ `'All Comments'`. Options: new `commentSortDefault` select control added to the UI Tweaks section in `options.html` (with description `<p>` noting it only fires when comments are already loaded); `options.js` uiMap entry added as `type: 'select'`.
- ✅ Phase 46: Orphaned Settings Activation — Three settings existed in `default-settings.js` as ON-by-default but had zero content-script implementation. (1) **`filters.removeGameInvites: true`**: New `removeGameInvitePosts(rootNode)` method in `cleaner.js`. Three-signal detection: pagelet name check (`/\bGaming\b|GameUnit/i` on `data-pagelet` — zero-cost, no querySelectorAll), link href check (`/\/gaming\/|\/games\/|apps\.facebook\.com\/|\.fbgamingapp\.com/i` on all `a[href]` in the post), and text-phrase check (game invite copy in 9 locales: EN "invited you to play" / "sent you a game request", + ES/FR/DE/IT/NL/SV/DA/NO equivalents — conservative, no false-positive "is playing" tokens). Reason: `'Game Invite/Post'`. Called from `_applyAllFilters` after `removePageSuggestions`. Toggle-OFF restoration guard added to `_restoreCriticalContainers`. Options: new toggle row in Core Filters card (`opt_filters_removeGameInvites`); `options.js` uiMap entry added. (2) **`filters.removeLargeReactions: true`**: CSS injected in `ui-tweaks.js` `update()` when setting is ON — targets `[data-pagelet^="FeedUnit_"] div[dir="auto"] > span[style*="font-size"]` and `[data-pagelet^="FeedUnit_"] [data-ad-comet-preview="message"] > div > span[style*="font-size"]` with `font-size: 1rem !important` — normalizes FB's oversized emoji rendering (where emoji-only posts get rendered at 3× normal size via inline style injection). Scoped to FeedUnit_ pagelets to avoid touching Messenger or other surfaces. Options: new toggle row in Clutter & Injections card (`opt_filters_removeLargeReactions`); `options.js` uiMap entry added. (3) **`uiMode.fixTimestamps: true`**: The implementation already existed in `ui-tweaks.js` (`_syncAbsoluteTimestamps`, `_upsertPostDateChip`, `_isLikelyTimestampAnchor`, `_extractAbsoluteTimestampText`) but was disabled by a "safety rollback" that called `_clearAbsoluteTimestampLabels()` unconditionally in `update()`. Fix: `update()` now calls `_syncAbsoluteTimestamps()` when `fixTimestamps` is ON (or `_clearAbsoluteTimestampLabels()` when OFF). `applyToNodes(nodes)` now calls `_syncAbsoluteTimestamps(nodes)` for new nodes when ON. The chip strategy: reads `aria-label` or `title` from post timestamp anchors (link validated by `_isLikelyTimestampAnchor` — 6 criteria including link href patterns, relative-time text regex, geometry position within 280px of post top, anchor width ≤ 220px), then injects a `.pf-post-date-chip` div immediately after the post heading container reading "Posted: [absolute date]". Toggle-OFF clears all chips via `_clearAbsoluteTimestampLabels`. Options: toggle was already wired in `options.html` and `options.js` — no changes needed.
- ✅ Phase 45: Hardening + Session Stats + v1.9.0 — Four items. (1) `messenger-main.js` `getEffectiveSettings()` disabled-state expanded to include all Phase 43 social settings: `alwaysShowMessageTimestamps`, `messengerMarkAllRead`, `messengerConversationFilter`, `detectUnsends` — all forced `false` when Messenger is disabled, matching the existing pattern for `hideMessengerTyping`/`messengerPrivacyBlur`. (2) Real popup session stats: `PureFusionApp` constructor gains `_sessionStats = { ads: 0, spam: 0 }` and `_sessionStatsFlushTimer`. `setupEventListeners()` adds a `window.addEventListener('pf:element_hidden', ...)` listener that tests `reason` against `/\bAd\b|Sponsored/i` — matching reasons increment `ads`, all others increment `spam`; a 600ms debounce timer flushes `{ ...this._sessionStats }` to `PF_Storage.setLocalData('pf_session_stats', ...)`. Popup already reads this key and displays the values — previously always 0 because nothing wrote it. (3) Version bump 1.8.1 → 1.9.0: `manifest.json` `version` field + `popup.html` `#pf-version` text. (4) `CHROME_STORE_LISTING.md` updated to v1.9.0: section 7 becomes new "Marketplace Local Filter" (6 bullet points), old sections 7-8 renumber to 8-9; "Messenger Privacy Tools" section expanded to "Messenger Enhancements" (5 bullets covering all 4 Phase 43 features + ghost mode); "Popup Feed Intelligence" section gains "Live session stats" bullet; reviewer test steps 10-12 added for Marketplace filter, Messenger enhancements, and AI (previously step 10-11).
- ✅ Phase 44: Marketplace Local Filter — New `PF_MarketplaceFilter` class in `marketplace-filter.js`. Self-gates on `_isMarketplacePage()` (`/^\/marketplace/i` path check) so it no-ops on all non-Marketplace pages at zero cost. On Marketplace: `_buildOverlay()` injects a fixed-position dark glass overlay (top-right, below the FB header) with: Max Distance range slider (5–100 mi, step 5; 100 = "No limit" / sort-only), "Local-first sort" checkbox (default on), "Hide unknown distance" checkbox. Distance parser `_parseDistanceMiles(listing)` uses a WeakMap cache keyed on the `<a>` element; matches "local pickup" → 0, `/\d+mi(?:les?)?/i` → miles, `/\d+km/i` → ×0.621371 miles, null for unknown. `_getListings()` selects `a[href*="/marketplace/item/"]`, deduplicates by href to handle featured+grid duplicates. `_applyFilter()` hides listings beyond threshold via `display:none` on the parent cell (grid wrapper div), applies CSS `order` for local-first sort on visible items — targeting the parent cell means the flexbox/grid layout reorders correctly. Count badge turns red (X/Y shown) when any are hidden. `_debouncedApplyFilter()` (450ms) invalidates parse cache then re-filters — called by MutationObserver on `[role="main"]` for infinite scroll compatibility. Slider value changes are persisted to `PF_Storage.updateSettings` async (fire-and-forget). `updateSettings` handles toggle-OFF by removing overlay + clearing filter + stopping observer. `destroy()` cleans up observer, overlay, and style element. Default settings: `marketplace: { enabled: false, maxDistanceMiles: 25, hideUnknownDistance: false }` in `default-settings.js` — deep-merged automatically into existing installs. Manifest: `marketplace-filter.js` added to facebook.com content_scripts list (before `inpage-ui.js`). `main.js`: module initialized in constructor, added to `_syncModuleSettings` modules array, `_runNodeProcessorsWithBudget` processor, `_runLiveResweepPass` sweepDocument call, and `_destroy` destroyable list; `getEffectiveSettings` disabled-state override sets `marketplace.enabled: false` when master toggle is OFF. Options: new "Marketplace Local Filter" card in Social tab with enable toggle, distance slider (with live preview label synced by `loadUIFromSettings`), and hide-unknown toggle. `options.js` uiMap: 3 entries (`checkbox`, `number` with fallback 25, `checkbox`); `loadUIFromSettings` triggers preview label sync after uiMap load. CSS: full overlay theme matching PureFusion dark palette; custom range slider thumb with purple gradient.
- ✅ Phase 43: Messenger Enhancements — Four independently-toggled features added to `PF_MessengerAI` in `messenger-ai.js`. (1) Always-visible timestamps: injects `pf-msg-timestamp-style` CSS targeting `[data-testid$="timestamp"]`, `[data-testid*="message_timestamp"]`, `time[datetime]` with `opacity/height/overflow/clip` overrides — best-effort across DOM versions. (2) Mark All Read: `_injectMarkAllRead` finds the Chats heading (`_findChatListHeader` scans `[role="heading"]/h2/h3` for "chats"/"messages"/"mensajes"/"nachrichten"/"berichten"), injects cyan `.pf-messenger-mark-read-btn`; click calls `_hideUnreadBadges` which hides `[data-testid="unread-count"]`, `[aria-label*="unread"]`, `[aria-label*="New message"]` — cosmetic only, no server interaction. (3) Conversation filter bar: `_injectConversationFilter` finds `[role="list"]` containing `/messages/` links, inserts `.pf-messenger-filter-bar` with All/Unread/Groups pill buttons before the list; `_applyConversationFilter` shows/hides `[role="listitem"]` rows using `_isConversationUnread` (checks `[data-testid="unread-count"]` and computed `font-weight ≥ 600`) and `_isGroupConversation` (multiple `img[alt]` elements or group aria-label regex); filter state persists in `_activeConvFilter` across re-injections. (4) Unsend detection: separate `unsendObserver` MutationObserver on `[role="main"]` watches for `removedNodes`; `_checkRemovedNode` validates the removed node has `[dir="auto"]` child, is within `[role="main"]`, and has 3–800 chars of text (skips tooltip/menu/dialog roles); inserts `.pf-unsend-placeholder` with timestamp and dismissable × button — session-only, nothing persisted. Constructor: `markAllReadBtn`, `filterBar`, `unsendObserver`, `_activeConvFilter`, `enhancementsDebounced` (600ms debounce) all initialized; `_applyEnhancements()` called after `scanDocument()`. `_startObserver` callback split: AI path guarded by `_isEnabled()`, enhancements path guarded by `_isAnyEnhancementEnabled()` — both run independently. `updateSettings` and `destroy` updated. CSS for all 4 features added to `_injectStyles`. Settings: `alwaysShowMessageTimestamps`, `messengerMarkAllRead`, `messengerConversationFilter`, `detectUnsends` (all `false` by default) added to `social` block in `default-settings.js`. Options: new "Messenger Enhancements" card in Social tab with 4 rows + description `<p>` elements. `options.js` uiMap: 4 new entries.
- ✅ Phase 42: Popup Feed Intelligence Section + Chrome Store Listing update — New `.pf-feed-intel-section` in popup (between stats and ad blocker) with: AI Scoring toggle (`tgl_aiScoring` → `settings.predictions.enabled`), live "X blocked / Y trusted" pill counts read from `pf_blocklist`/`pf_allowlist`, "Manage Sources" button opens options page. Toggle wired into `quickToggles` (respects master toggle disabled state). `updateFeedIntelUI()` updates subtitle text and applies `.intel-active` class (cyan tint when ON vs purple tint when OFF). `loadIntelCounts()` async-reads both local storage keys on popup open. CSS: full `.pf-feed-intel-*` system with `.intel-active` transition, red/green `.pf-intel-pill` variants, ghost `.pf-intel-manage-btn`. Also: `CHROME_STORE_LISTING.md` fully rewritten for v1.8.1 — covers all phases 37-42 features (AI classification, blocklist/allowlist, session filters, Source Manager, Feed Intelligence popup, Zap), updated reviewer test steps including Insight Chip walkthrough and Source Manager verification, `contextMenus` added to permission justifications, all section headings converted from bold paragraphs to `###` to fix MD036 linter warnings.
- ✅ Phase 41: AI Source Manager UI — New card in Options → Keyword Filters tab. `loadSourceManager()` reads `pf_blocklist` and `pf_allowlist` directly from local storage (separate from keyword-based sourceBlocklist). `renderSourceList()` dynamically builds a scrollable list of author entries each with an individual "Remove" button. "Clear All" with confirmation dialog on both lists. Manual "Add" input with Enter key support. Count badge (e.g. "3 blocked") shown in section header. `addToSourceList()` deduplicates before writing. CSS: `.pf-btn-sm` / `.pf-btn-xs` size variants; `.pf-source-manager-*` layout; `.pf-source-row` card style; `.pf-source-count` pill badge; scrollable `.pf-source-list` (max 240px). No predictor.js changes — pure options-page feature.
- ✅ Phase 40: Word-Boundary Classification + Persistent Allowlist — `_tokenMatch(text, token)` helper: single-word tokens check left/right char boundaries (`!/[a-z0-9]/` on both sides); multi-word tokens fall through to plain `includes`. All 5 classification loops now use `_tokenMatch` — eliminates false positives ('bill' in 'billboard', 'policy' in 'policy-making' substring matches). Removed 'bill' + 'cabinet' from politicalTokens (phone bill / kitchen cabinet ambiguity survives even with boundary check); removed 'exclusive' from newsTokens (too easily confused with commercial language). `this.allowlist` (Set) loaded from `pf_allowlist` local storage on `init()`. `_isAllowlisted(node)` checks author against allowlist. `_processSingleNode` step 5 and `_refreshPredictionDecorations` both exempt allowlisted authors from session content-type filters. "Always show source" action now adds to `this.allowlist` + calls `_saveAllowlist()` + triggers `sweepDocument(true)` (previously only boosted engagement score). "Unallow source" button: removes from allowlist + saves + sweeps. Chip classification row gains green `Trusted Source` badge when author is in allowlist. Details panel shows "Trusted Source" status section (green) with "Remove from trusted sources" button + "Already trusted" disables the "Always show" Quick Action button. `allowlistSize` counter shown in Quick Actions header. `predictVersion` bumped to `v13-allowlist`.
- ✅ Phase 39: Persistent Author Blocklist — "Block source" now permanent (`this.blocklist` Set loaded from `pf_blocklist` local storage key on init; blocklist check runs before all scoring in `_processSingleNode` for zero-cost early exit; `_refreshPredictionDecorations` re-hides blocked authors on throttled re-check; `_clearPredictionDecorations` temporarily restores so forceRescore can re-evaluate; "Block source" adds to blocklist + saves to storage + hides immediately + sweeps; "Unblock source" button in chip Details "Source Status" section restores author and resets engagement penalty; "Block source" button disabled/greyed when author already blocked; blocklist count shown in Quick Actions header; `_saveBlocklist()` fire-and-forget helper; `predictVersion` bumped to `v12-persistent-blocklist` forcing re-evaluation of all existing nodes)
- ✅ Phase 38: Session Content-Type Filter — "Hide similar posts" functional implementation (`sessionContentFilters` Set; filter applied in `_processSingleNode` step 5 after Model F classification; `_refreshPredictionDecorations` early-exit for session-filtered nodes; `_clearPredictionDecorations` temporarily restores before resweep; `_handleInsightAction` "hide-similar" adds to Set + calls `sweepDocument(true)` for immediate feed-wide effect; "reset-session-filters" clears Set + resweeps; active filters shown in Details panel with reset button; amber reset button style)
- ✅ Phase 37: Insight Chip UX Overhaul — Content Classification + Action Hooks (Model F `_classifyContentType` with political/opinion/news/commercial/emotional token banks; `_resolveUnifiedInsightState` now returns context-aware labels: 'Opinionated Political Content', 'Political / News Content', 'Opinion / Editorial', 'News / Report', 'Possible Promotional Content', 'Mixed Signals' — 'Neutral Relevance' eliminated; new `pf-insight-info` purple severity; classification badge row on chip; Quick Actions panel in Details: Hide similar / Always show source / Block source; `_handleInsightAction` handler)
- ✅ Phase 36: Unimplemented Filter Activation — Page Suggestions + Suggested Subheader Detection (`removePageSuggestions` was wired in UI/presets but had zero content-script implementation; `_hasSuggestedSubheaderLabel` new 9-locale method scans the post header zone ≤120px for "Suggested for you" / "Recommended for you" tokens post-normalized across all locales; added to `removeSuggestedPosts` as a text-based fallback for untagged injections; new `removePageSuggestions()` method targets speculative `data-pagelet` names + same text heuristic with distinct reason string `"Page Suggestion"` for independent toggle-OFF restoration; `_applyAllFilters` gated; `_restoreCriticalContainers` guard added)
- ✅ Phase 35: Toggle-OFF Restoration Hardening — all filter categories now correctly unhide content when their governing setting is turned off. Added DoD-compliant guards to `_restoreCriticalContainers` for: Suggested Posts / PYMK / Suggested Groups / Reels / Stories / Marketplace / Fundraisers (simple toggles); all 7 Post Type filters; all 14 Story Activity Type filters; all 6 Image Subject filters. `removeSuggestedPosts` expanded with direct `[data-pagelet^="SuggestedUnit_"]` targeting for non-FeedUnit_ suggested injections.
- ✅ Phase 34: Ad Detection Hardening — AdUnit Direct Targeting + Separator Normalization (`_removeAdsByHardSignals` Step 1 directly targets `[data-pagelet^="AdUnit_"]` containers — zero false positives, hides the wrapper not just the inner article, prevents blank-space artifacts; Step 2 skips articles already covered by Step 1, adds `[data-testid="ad_boundary"]`; `_isSponsoredLabel` normalizes U+2022 BULLET and U+00B7 MIDDLE DOT to ` · ` before pattern matching — covers FB locales/variants that use bullet as separator)
- ✅ Phase 33: Feed Mode Presets — New Settings Backfill (`hideMemories: true` in clean/focus/ultrafast/classic; `sidebar` block added to focus/ultrafast/classic; Meta AI + Manus AI sidebar hidden in focus/ultrafast; left/right Meta AI hidden in clean/classic)
- ✅ Phase 32: Sponsored Toggle-OFF Restoration + Popup Wiring (`_restoreCriticalContainers` handles `'Ad (Hard Signal)'` + `'Sponsored Post (Label Heuristic)'` + legacy `'Sponsored Post (Heuristic)'` reason strings; browser popup gains `tgl_removeSponsored` secondary row in Ad Blocker section; popup.js reads/writes `filters.removeSponsored`)
- ✅ Phase 31: Sponsored/Ads Split — Full Surface Wiring (options page `opt_filters_removeSponsored` + `uiMap` entry; popup "Ad Blocker" title + description updated; i18n keys updated EN+ES; popup.js `removeSponsored` preservation comment)
- ✅ Phase 30: Sponsored Label Detection Hardening (`_isSponsoredLabel` mid-string/endsWith patterns; `_findSponsoredMarkerInPost` 260→400px / 32→48 char thresholds; `aria-describedby` scan added; `[role="link"]` aria-label selectors; `adabouturl` + lowercase `_cft_%5b0%5d` URL patterns)
- ✅ Phase 29: Image Subject Filter Hardening (FR/DE/IT/NL/SV/DA/NO tokens for all 6 categories; false-positive audit removes 'outdoor'+'building' from travel; prefix-stripping regex expanded for all 9 locales)
- ✅ Phase 28: Granular Sidebar Hardening (position-aware `_resolveRightSidebarContainer`; all heading/aria-label tokens expanded to 9 locales for Trending/Contacts/Events/Birthdays; `_hasShortcutsHeading` 9-locale; `_looksLikeContactsModule` 9-locale)
- ✅ Phase 27: UI Tweaks — Unimplemented Settings Activation (fontSizeScale CSS, anonymizerMode hover-reveal blur, disableCommentAutofocus guard)
- ✅ Phase 26: Notification + Search Popup Full Locale Expansion (FR/DE/IT/NL/SV/DA/NO for all 4 notif categories + search input selectors + trending/recent tokens)
- ✅ Phase 25: Memories Filter + Messenger Lifecycle Guard (removeMemoriesPosts; toggle-OFF reversal; messenger-main lifecycle guard)
- ✅ Phase 24: Content Script Lifecycle Hardening (chrome.runtime.connect port guard; destroy() on all modules with intervals/observers)
- ✅ Phase 23: Sponsored Detection Locale Completion (NL/SV/DA/NO aria-label selectors; token-driven right-rail text scan)
- ✅ Phase 22: Options UI Hardening & Locale Settings (filterLocale setting; locale-aware gate filters; i18n orphan fixes)
- ✅ Phase 21: Post-type Filter Tuning (NL/SV/DA/NO locale expansion; gate filter diacritic correctness hardened)
- ✅ Phase 20: Visual Polish & Theme Expansion (AMOLED refined; Pastel added; Insight Chip propagation)
- ✅ Phase 19: Filter Logic Consolidation (shared helpers; duplicate filter removal; locale token merge)
- ✅ Phase 18: Performance Pass (Chunked Cleaning + Skip Markers)
- ✅ Phase 17: Auto Comment Preview v3.0 (DOM injection strategy — shell injection DONE; v3.1 real data deferred)
- ✅ Phase 16: Multilingual & Story Hardening (NL/SV/DA/NO + Body Text Guard)
- ✅ Phase 15: Context Menu "Zap" (Instant rules + Undo support)
- ✅ Phase 13: Power-User Rule Engine (Selector + Text logic)

**Do NOT jump ahead to:**

- Auto Comment Preview v3.1 (needs explicit go decision)
- Plugin SDK (needs explicit go decision)
- Non-critical UI cleanup

**No phases currently queued.**

---

## Last Action Log

- **Last completed (2026-04-16): Phase 51 — Lifecycle + Disabled-State Hardening.** Three correctness gaps. (1) `ui-tweaks.js` gained `destroy()`: removes DFM capture keydown, removes `pf-dfm-active` class, calls `_restoreWrappedLinks()` + `_clearAbsoluteTimestampLabels()`, removes `<style>` element. (2) `predictor.js` gained `destroy()`: clears interval, removes visibilitychange + click listeners, cancels `_friendBadgeDebounce`, removes `.pf-friend-unseen` badges, removes `<style id="pf-predictor-styles">`. (3) Both modules added to `_destroy()` destroyable list in `main.js`. (4) `getEffectiveSettings()` expanded: `filters` gains 7 new false-overrides; `uiMode` gains 11 new overrides (incl. `distractionFreeMode`, `showLinkPreviews`, `fixTimestamps`, `compactMode`, `widescreenMode`, `commentSortDefault: 'All Comments'`); `predictions` gains 7 new false-overrides (incl. `trueAffinitySort`, `showFriendActivity`); new `sidebar.enableModuleFilters: false` block added.
- **Last completed (2026-04-16): Phase 50 — v2.0.0 + Popup DFM Toggle + Changelog.** (1) Popup: `tgl_distractionFree` checkbox added to Quick Filters section in `popup.html`; fully wired in `popup.js` (element map, `quickToggles` inclusion, init from `settings.uiMode.distractionFreeMode`, `handleToggle` write, `addEventListener`). (2) Version bumped 1.9.0 → 2.0.0 in `manifest.json` and `popup.html` fallback. (3) `CHANGELOG.md` v2.0.0 entry added covering all phases 45-49 features; MD032 blank-line-around-lists compliance applied.
- **Last completed (2026-04-16): Phase 49 — Friend Activity Feed Insight.** Orphaned `predictions.showFriendActivity: true` fully activated. (1) `_sessionFeedAuthors` Set and `_friendBadgeDebounce` timer added to `PF_Predictor` constructor. (2) `_processSingleNode`: when setting is ON, adds the normalized post author name to `_sessionFeedAuthors` as each post is processed. (3) `sweepDocument()`: triggers `_applyFriendActivityBadges()` via 800ms debounce after every sweep. (4) New `_applyFriendActivityBadges()`: locates contacts panel by aria-label across 8 locales; iterates `[role="listitem"]` rows; injects/removes `.pf-friend-unseen` amber pill badge on contacts not seen as post authors; guards against false noise by requiring ≥3 seen authors before badging; uses exact + first-name prefix matching to handle full-name vs display-name discrepancies. (5) `.pf-friend-unseen` CSS added to `_injectPredictorStyles`. (6) Options toggle + uiMap wired.
- **Last completed (2026-04-16): Phase 48 — True-Affinity Sort Options UI + Distraction-Free Reading Mode.** Two orphaned settings activated. (1) `trueAffinitySort`: Options UI added — `auto-height` toggle row in the AI Predictions card (after `opt_pred_showTrending`) with description explaining the CSS flexbox `order` approach. `opt_pred_trueAffinitySort` uiMap entry added to `options.js`. The predictor.js implementation (`_applyNativeAffinitySort`) was already complete — no content-script changes needed. (2) `distractionFreeMode`: Full implementation in `ui-tweaks.js`. `_dfmActive` (bool) and `_dfmKeyHandler` (function ref) added to constructor. `_setupDistractionFreeMode()` called from `init()` — sets `_dfmActive` from settings, calls `_applyDfmClass()`, registers a capture-phase `keydown` listener for Alt+Shift+F that toggles the mode. `_applyDfmClass()` adds/removes `pf-dfm-active` on `document.documentElement`. CSS in `update()` (before `styleTag.textContent`): hides `[data-pagelet="LeftRail"]`, `[data-pagelet="RightRail"]`, `[role="complementary"]`, non-primary `[role="navigation"]`; constrains `[role="main"]` to `max-width: 680px` centered. `updateSettings()` now syncs `_dfmActive` from settings before calling `update()` so the options checkbox controls the initial state and live pushes work correctly. Options: new `auto-height` toggle row in UI Tweaks section (before Theme `<hr>`, after `commentSortDefault`); `opt_uiMode_distractionFreeMode` uiMap entry in `options.js`.
- **Last completed (2026-04-16): Phase 47 — Link Destination Reveal + Comment Sort Enforcement.** Two orphaned `uiMode` settings activated. (1) `showLinkPreviews`: `_revealLinkDestinations` rewrites `l.facebook.com/l.php` hrefs to the real `u=` destination, storing originals in `data-pf-original-href`; `_restoreWrappedLinks` reverses on toggle-OFF; wired into `applyDocumentLevelTweaks`, `applyToNodes`, and the `update()` toggle-OFF path. (2) `commentSortDefault`: `_enforceCommentSort` + `_findCommentSortButton` enforce a user-selected sort (Newest / Top Comments) via delayed click simulation (350ms + 200ms) on identified sort buttons; per-article `data-pf-sort-enforced` marker prevents re-entry. `commentSortDefault` select control added to options UI.
- **Last completed (2026-04-15): Phase 46 — Orphaned Settings Activation.** Three on-by-default settings that existed in `default-settings.js` but did nothing were implemented. (1) `filters.removeGameInvites`: New `removeGameInvitePosts(rootNode)` in `cleaner.js` with 3-signal detection (pagelet name → href scan → 9-locale text-phrase check). Called from `_applyAllFilters` after `removePageSuggestions`. Toggle-OFF guard added to `_restoreCriticalContainers`. Two new options rows wired. (2) `filters.removeLargeReactions`: CSS injected in `ui-tweaks.js` `update()` targeting `[data-pagelet^="FeedUnit_"] div[dir="auto"] > span[style*="font-size"]` with `font-size: 1rem !important` to normalize FB's 3× oversized emoji rendering. New option row wired. (3) `uiMode.fixTimestamps`: Existing implementation in `ui-tweaks.js` (`_syncAbsoluteTimestamps` / `_upsertPostDateChip`) was disabled by a hard-coded safety rollback — replaced with conditional call on `fixTimestamps` flag in both `update()` and `applyToNodes()`. Option was already wired in HTML/JS.
- **Last completed (2026-04-15): Phase 45 — Hardening + Session Stats + v1.9.0.** Four items shipped. (1) `messenger-main.js` `getEffectiveSettings()` disabled-state block for `effective.social` expanded with 4 Phase 43 keys (`alwaysShowMessageTimestamps`, `messengerMarkAllRead`, `messengerConversationFilter`, `detectUnsends` — all `false` on disable). (2) Real session stats: `_sessionStats = { ads: 0, spam: 0 }` and `_sessionStatsFlushTimer` added to `PureFusionApp` constructor; `setupEventListeners()` adds a `window` listener for `pf:element_hidden` that routes reasons matching `/\bAd\b|Sponsored/i` to `ads`, all others to `spam`, with a 600ms debounce before writing to `PF_Storage.setLocalData('pf_session_stats', ...)`. (3) Version bump to `1.9.0` in `manifest.json` and `popup.html`. (4) `CHROME_STORE_LISTING.md` updated to v1.9.0: new §7 Marketplace Local Filter, §8 Optional AI (renumbered), §9 Messenger Enhancements (expanded from old Privacy Tools), session stats bullet added to §4 Popup Intelligence, reviewer steps 10-12 added for Marketplace + Messenger + AI.
- **Last completed (2026-04-15): Phase 44 — Marketplace Local Filter.** New standalone module `marketplace-filter.js` (new file). Self-gates via `_isMarketplacePage()` (`/^\/marketplace/i` pathname test) — no-ops on all non-Marketplace pages at zero cost. On Marketplace pages: injects a fixed-position dark glass overlay (top-right, 72px below top to clear FB header) containing: (1) Max distance range slider (5–100 mi, step 5; 100 displays as "No limit" and skips the distance filter so only local-first sort runs), (2) "Local-first sort" checkbox (default checked), (3) "Hide unknown distance" checkbox, (4) a live count badge that turns red when listings are hidden. Distance parser `_parseDistanceMiles(listing)` caches per `<a>` element in a WeakMap — matches "local pickup" → 0 mi (always shown), `\d+mi(?:les?)?\b` → miles, `\d+km\b` → ×0.621371 mi, `null` for unknown. `_getListings()` queries `a[href*="/marketplace/item/"]` and deduplicates by href to handle cards that appear in both featured and grid positions. `_applyFilter()` operates on the parent cell wrapper div (not the `<a>` itself) for `display:none` and `order` properties — applying to the wrapper ensures the flexbox/grid layout reorders correctly regardless of whether the `<a>` or its parent is the flex item. Distance threshold and sort are read live from the overlay controls. MutationObserver on `[role="main"]` (childList + subtree) triggers `_debouncedApplyFilter()` (450ms, clears parse cache first) for infinite-scroll compatibility. Slider/checkbox changes call `PF_Storage.updateSettings` async. `updateSettings` handles toggle-OFF by removing overlay, clearing all filters (restores display/order), and disconnecting observer. `destroy()` cleans up observer, overlay, and `<style>` element. CSS: full dark glass theme (backdrop-filter blur, purple-gradient slider thumb, cyan count badge, red badge when filtering active). Default settings: `marketplace: { enabled: false, maxDistanceMiles: 25, hideUnknownDistance: false }` added to `default-settings.js` — `_deepMerge` in storage.js handles it automatically for existing installs. Manifest: `marketplace-filter.js` added to `*://*.facebook.com/*` content_scripts list (before `inpage-ui.js`). `main.js` hooks: constructor, `_syncModuleSettings` modules array, `_runNodeProcessorsWithBudget` processor entry, `_runLiveResweepPass` sweepDocument call, `_destroy` destroyable list; `getEffectiveSettings` disabled-state override sets `marketplace.enabled: false` when master toggle is OFF. Options: new "Marketplace Local Filter" card in Social tab with 3 controls; `options.js` uiMap: 3 entries (`checkbox`, `number/fallback:25`, `checkbox`); `loadUIFromSettings` syncs the slider preview label on page load.
- **Last completed (2026-04-15): Phase 43 — Messenger Enhancements.** Four independently-toggled features shipped inside the existing `PF_MessengerAI` class (`messenger-ai.js`): (1) **Always-visible timestamps** — injects `<style id="pf-msg-timestamp-style">` targeting `[data-testid$="timestamp"]`, `[data-testid*="message_timestamp"]`, `time[datetime]` with `opacity/height/overflow/visibility/clip` overrides; created on toggle-ON, removed on toggle-OFF. (2) **Mark All Read button** — `_injectMarkAllRead` scans headings for "chats"/"messages"/"mensajes"/"nachrichten"/"berichten", appends a cyan `.pf-messenger-mark-read-btn` button; click calls `_hideUnreadBadges` which hides `[data-testid="unread-count"]`, `[aria-label*="unread"]`, `[aria-label*="New message"]` elements — cosmetic only, no FB server interaction. (3) **Conversation filter bar** — `_injectConversationFilter` finds `[role="list"]` container with multiple `/messages/` links and inserts a `.pf-messenger-filter-bar` with All/Unread/Groups pills before it; `_applyConversationFilter` shows/hides `[role="listitem"]` rows via `_isConversationUnread` (unread badge elements + computed `font-weight ≥ 600` check) and `_isGroupConversation` (multiple `img[alt]` avatars or group regex on link `aria-label`); active filter preserved in `_activeConvFilter` across re-injections; turns restores all rows on toggle-OFF. (4) **Unsend detection** — separate `unsendObserver` (`MutationObserver` on `[role="main"]`, subtree) watches `removedNodes`; `_checkRemovedNode` validates the removed node has `[dir="auto"]` child (FB message marker), is within `[role="main"]`, 3–800 chars (skips tooltip/menu/dialog/banner/navigation roles); appends `.pf-unsend-placeholder` with localized timestamp and dismissable × button — session-only, nothing stored. Constructor additions: `markAllReadBtn`, `filterBar`, `unsendObserver`, `_activeConvFilter`, `enhancementsDebounced` (600ms) all initialized; `_applyEnhancements()` called at end of constructor. `_startObserver` callback restructured: removed top-level `if (!_isEnabled()) return` gate — now checks `_isEnabled()` for AI scan and `_isAnyEnhancementEnabled()` for enhancements independently. `updateSettings` calls `_applyEnhancements()`. `destroy` disconnects `unsendObserver`, removes `markAllReadBtn`, `filterBar`, and timestamp style element. CSS: all 4 feature UI components added to `_injectStyles`. Default settings: 4 new keys under `social` block (`alwaysShowMessageTimestamps`, `messengerMarkAllRead`, `messengerConversationFilter`, `detectUnsends`, all `false`). Options page: new "Messenger Enhancements" card in Social tab with 4 toggles + descriptive `<p>` elements. Options.js uiMap: 4 new entries wired.
- **Last completed (2026-04-15): Phase 42 — Popup Feed Intelligence + Chrome Store Listing.** Two deliverables. (1) Popup Feed Intelligence section: new card inserted between the stats dashboard and ad blocker. Contains an AI Scoring toggle (`tgl_aiScoring`) wired to `settings.predictions.enabled` — previously only accessible from Full Settings → Predictions tab. Live blocked/trusted pill counts loaded async from `pf_blocklist`/`pf_allowlist` local storage on popup open. "Manage Sources" button opens the options page. `updateFeedIntelUI()` updates the subtitle description and applies `.intel-active` CSS class (cyan border + tint when ON, purple when OFF). `loadIntelCounts()` populates the pill labels. Toggle added to `quickToggles` array so it greys out when the master toggle is OFF. CSS: `.pf-feed-intel-section` with transition, `.pf-intel-pill-block` (red), `.pf-intel-pill-trust` (green), `.pf-intel-manage-btn` ghost cyan link style. (2) `CHROME_STORE_LISTING.md` rewritten: 8 numbered feature sections (Feed Cleanup, AI Scoring, Source Control, Popup Intelligence, Settings, Multilingual, BYOK AI, Messenger Privacy), updated reviewer steps covering Insight Chip + Source Manager walkthrough, `contextMenus` added to permission justifications, all `**bold paragraph**` section headers converted to `### heading` to fix MD036 linter warnings.
- **Last completed (2026-04-15): Phase 41 — AI Source Manager UI.** Gap closed: the Insight Chip's persistent blocklist and allowlist had no management surface outside of Facebook — users could add authors one at a time from individual posts but had no way to see the full list or bulk-clear it. New card added at the bottom of Options → Keyword Filters tab. `loadSourceManager()` reads `pf_blocklist` + `pf_allowlist` directly from `PF_Storage.getLocalData` (these are stored raw as string arrays, separate from the main settings object's keyword-based `sourceBlocklist`/`allowlistFriends`). `renderSourceList()` builds a scrollable list (max-height 240px with overflow-y: auto) of author name rows, each with a `.pf-btn-xs` "Remove" button that fires `PF_Storage.setLocalData` and re-renders. "Clear All" has a confirmation dialog that states the count before wiping. Manual entry input supports Enter key via keydown handler; `addToSourceList()` deduplicates before pushing. Section header shows a purple pill count badge. CSS additions: `.pf-btn-sm` (12px, 5px 12px padding) + `.pf-btn-xs` (11px, 3px 9px) button size variants; full `.pf-source-manager-*` layout system. No changes to predictor.js or any content script.
- **Last completed (2026-04-15): Phase 40 — Word-Boundary Classification Accuracy + Persistent Allowlist.** Two core improvements shipped together. (1) Token matching: new `_tokenMatch(text, token)` helper uses `indexOf` + boundary walk — checks that the character before and after each match is not `[a-z0-9]`, preventing tokens like 'bill' from matching 'billboard' or 'ability'. Multi-word tokens bypass the boundary check (spaces are natural delimiters). All 5 classification score loops in `_classifyContentType` now call `_tokenMatch` instead of `t.includes`. Removed 'bill' and 'cabinet' from `politicalTokens` outright (phone bill / kitchen cabinet remain ambiguous even with boundary guards); removed 'exclusive' from `newsTokens` (overlaps commercial promotional language). (2) Allowlist: `this.allowlist` (Set) loaded from `pf_allowlist` local storage key on `init()`. `_isAllowlisted(node)` is the single lookup helper. `_processSingleNode` step 5 + `_refreshPredictionDecorations` both wrap their session-filter hide behind `!this._isAllowlisted(node)` — trusted sources are never hidden by "Hide similar posts", regardless of content type. "Always show source" action upgraded: now adds to `this.allowlist` + calls `_saveAllowlist()` (fire-and-forget) + calls `sweepDocument(true)` so the post reappears immediately; previously only boosted engagement score. New "unallow-source" action removes from allowlist + saves + sweeps. Chip classification row: green `Trusted Source` badge rendered when author is allowlisted. Details panel: "Trusted Source" status section (green border) with "Remove from trusted sources" amber button. Quick Actions: "Always show source" button shows as "Already trusted" (disabled) when allowlisted; `allowlistSize` count shown inline in section title. CSS: `.pf-insight-trusted-badge` (green pill), `.pf-insight-trusted-section` (green border top). `predictVersion` bumped to `v13-allowlist`.
- **Last completed (2026-04-15): Phase 39 — Persistent Author Blocklist.** "Block source" is now permanent across page reloads instead of just penalizing the engagement score. `this.blocklist` (Set) loaded from `pf_blocklist` in local storage on `init()`. `_processSingleNode` checks the blocklist immediately after the version-string gate, before `_analyzeForTrends` and `_scorePost` — blocked authors cost zero processing time. Blocked posts get `pfBlocked=true` and `display:none !important`. `_refreshPredictionDecorations` re-hides on the 2s throttled refresh pass. `_clearPredictionDecorations` temporarily removes the hide so `forceRescore` can re-classify and re-hide. "Block source" action: adds to `this.blocklist`, calls `_saveBlocklist()` (fire-and-forget `PF_Storage.setLocalData`), hides current post immediately, calls `sweepDocument(true)` to catch all other posts from that author currently in the feed. "Unblock source" action (new): removes from blocklist, saves, resets engagement profile to 0, sweeps to restore. Details panel shows "Source Status" blocked section with "Unblock source" button (amber) when author is blocked. "Block source" button in Quick Actions is disabled and labelled "Already blocked" when the author is in the blocklist. Blocklist count shown inline in Quick Actions section title. `predictVersion` bumped to `v12-persistent-blocklist` to force re-evaluation of all existing nodes on first load after update.
- **Last completed (2026-04-15): Phase 38 — Session Content-Type Filter.** "Hide similar posts" action button now actually hides matching posts instead of just toasting. `this.sessionContentFilters` (Set, initialized in constructor) stores content type strings. `_processSingleNode` step 5: after Model F classification, if the post's `pfContentType` is in the Set, the post is `display:none !important` + `pfSessionFiltered=true`. `_refreshPredictionDecorations`: same early-exit for already-processed nodes so they're caught on the throttled re-check pass. `_clearPredictionDecorations`: session-filtered posts temporarily restored so the next `_scorePost` can re-classify and re-hide them (ensures correct behavior on forceRescore). `_handleInsightAction` "hide-similar": adds contentType to the Set, calls `this.sweepDocument(true)` for immediate feed-wide effect, toast says "Reload page to reset". New "reset-session-filters" action: clears the Set, re-sweeps to restore all hidden posts. Details panel shows "Session Filters Active" section with the list of active filters and the amber "Reset (restore hidden posts)" button when any filters are set. CSS added for `.pf-insight-action-btn-reset` (amber) and `.pf-insight-session-section`.
- **Last completed (2026-04-15): Phase 37 — Insight Chip UX Overhaul.** "Neutral Relevance" eliminated. New Model F (`_classifyContentType`): 5 token banks (political ~30 EN/DE/FR/ES terms, news ~20 terms, opinion ~25 terms, commercial ~15 terms, emotional ~18 terms); scores each category independently; contentType priority: political > opinion > news > commercial > personal; tone derived from score dominance: Opinionated / Informational / Emotional / Political framing / Promotional / Neutral; confidence = Low/Medium/High from total signal count. `_resolveUnifiedInsightState` now routes through classification after hard signals: mid-range scores that previously fell through to "Neutral Relevance" now get context-aware labels. New `pf-insight-info` severity (purple `rgba(40,28,72,0.94)`) for Opinion/Political Opinion. Classification badge row (`pf-insight-classification`) visible when confidence >= Medium + contentType != Personal — shows type-badge + tone-badge + confidence label inline on chip. Quick Actions section added to Details panel (always present): "Hide similar posts" (toast + disable), "Always show source" (boosts author engagement profile by +10 reactions, persists to local storage), "Block source" (hard-sets profile to -20 across all engagement axes). `_handleInsightAction` delegates action button clicks via existing document-level click handler. Classification dataset attrs (`pfContentType`/`pfContentTone`/`pfContentConfidence`) cleared on `_clearPredictionDecorations` and forceRescore sweep.
- **Last completed (2026-04-15): Phase 36 — Page Suggestions + Suggested Subheader Detection.** `removePageSuggestions` existed in default-settings, options UI, and all four non-custom presets but had zero content-script implementation — the toggle did nothing. Fixed: `_applyAllFilters` now calls `this.removePageSuggestions(rootNode)` when the setting is ON. New `removePageSuggestions()` targets speculative pagelet names (`SuggestedPage`, `PageSuggestion`, `LikePageUnit`) and runs the new `_hasSuggestedSubheaderLabel` heuristic with reason `"Page Suggestion"`. New `_hasSuggestedSubheaderLabel(postNode)`: 9-locale token array (EN/ES/FR/DE/IT/NL/SV/DA/NO) for "Suggested for you" and "Recommended for you" in post-normalized form; scans spans/links/divs within ≤120px of the article top; handles off-screen/background-tab zero-rect edge case; middle-dot separator variant covered. Also added as text-based fallback to `removeSuggestedPosts` (reason "Suggested Posts") for injection patterns without known pagelet names. `_restoreCriticalContainers` guard: `reason === "Page Suggestion" && !f?.removePageSuggestions`.
- **Bug fix (2026-04-15): Lifecycle guard false-positive destroy — FAB/post inspector vanishing.** Root cause: commit `c217c7c9e9cbfbbecd333b5bb32372f04cc02547` introduced `_startLifecycleGuard()` which calls `_destroy()` on port disconnect. In MV3, Chrome terminates the background service worker after ~30 s of inactivity — this disconnects ALL ports, firing `onDisconnect` even though the extension is fully alive. The content script incorrectly treated idle SW termination as extension invalidation, destroying the FAB docking loop and nulling the observer. Fix: `onDisconnect` now checks `chrome.runtime.id` — if still defined, the extension is alive (SW just idle) and we reconnect via `setTimeout` without destroying; only destroy when `chrome.runtime.id` is undefined (true invalidation).
- **Last completed (2026-04-15): Phase 35 — Toggle-OFF Restoration Hardening.** `_restoreCriticalContainers` previously only restored 4 reason-string categories (nav, Meta AI, Memories, Ads, Sponsored). All remaining filter categories were missing — posts hidden by Suggested/PYMK/Reels/Stories/Marketplace/Fundraisers/PostTypes/StoryTypes/ImageSubjects would persist in the DOM after their toggle was turned off (DoD regression rule #3 violation). Added comprehensive lookup-table guards covering 7 simple feed filters, 7 post type filters, 14 story activity type filters, and 6 image subject filters. Local `_pfUnhide()` helper reduces repetition. `removeSuggestedPosts` gains a direct `[data-pagelet^="SuggestedUnit_"]` sweep for injection patterns that don't use the `FeedUnit_Suggested_For_You` pagelet name.
- **Last completed (2026-04-14): Phase 34 — Ad Detection Hardening.** `_removeAdsByHardSignals` restructured into two steps: Step 1 directly queries `[data-pagelet^="AdUnit_"]` containers and hides them at the wrapper level (not the inner article), preventing blank-space artifacts and covering all ad formats with zero false positives; Step 2 retains the article-level scan for ads not inside an AdUnit_ pagelet, skips articles already covered by Step 1, and adds `[data-testid="ad_boundary"]` to the signal list. `_isSponsoredLabel`: separator normalization pass — replaces U+2022 BULLET (•) and U+00B7 MIDDLE DOT (·) with canonical ` · ` form before pattern matching, so "Page • Sponsored" variants (used by some FB locales) are now detected.
- **CWS Rejection Fix (2026-04-15):** Hide Reels — Chrome Web Store rejection (violation: "Hide Reels" non-functional). Root cause: `popup.js` initialized `tgl_hideReelsStories` with `hideReels && hideStories` (AND condition); since `hideStories` defaults `false`, popup always showed toggle OFF even though `hideReels: true` was active. Reviewer saw OFF → concluded feature broken. Fixes: (1) popup init changed to `!!settings.filters.hideReels`; (2) save handler decoupled from `hideStories`; (3) popup toggle renamed "Hide Reels" (not "Hide Reels & Stories"); (4) `reelsTray` selector expanded with `aria-label*="Reels" i`, `[role="region"]` structural, lowercase pagelet variant; (5) `removeReelsTray` heuristic expanded: new aria-label pass, regex `^reels\b` guard, `data-pagelet` ancestor walk.
- **Last completed (2026-04-15):** Phase 33 — Feed Mode Presets New Settings Backfill. All four non-custom modes patched with settings added since Phase 17 that were never wired into presets: `filters.hideMemories: true` added to clean/focus/ultrafast/classic (memories clutter = relevant to every "cleaner" mode). `sidebar` block added to focus (full Meta AI + Manus AI left/right hidden), ultrafast (same), clean and classic (Meta AI left nav + right contact hidden, trending hidden). `smart` mode intentionally unchanged — it uses intelligent curation rather than blanket hiding.
- **Prior (2026-04-15):** Phase 32 — Sponsored Toggle-OFF Restoration + Popup Wiring. `_restoreCriticalContainers`: added three reason-string guards before the bail-out check — restores `'Ad (Hard Signal)'` nodes when `removeAds` is OFF; restores `'Sponsored Post (Label Heuristic)'` and legacy `'Sponsored Post (Heuristic)'` nodes when `removeSponsored` is OFF. Fixes DoD regression rule #3 (toggle-OFF must restore content). Browser popup: added secondary `pf-ad-blocker-row` for `tgl_removeSponsored` with 🔍 icon; `popup.js` maps it into `elements.sponsored`, adds it to `quickToggles`, reads initial state from `filters.removeSponsored`, and writes it back in `handleToggle`. i18n: `popup_hide_sponsored_posts` + `_desc` keys added EN+ES.
- **Prior (2026-04-14):** Phase 31 — Sponsored/Ads Split Full Surface Wiring. `options.html`: added `opt_filters_removeSponsored` row (label: "Hide Sponsored Posts (label detection)") immediately after `opt_filters_removeAds`. `options.js` `uiMap`: added `opt_filters_removeSponsored → filters.removeSponsored`. `popup.html`: "Sponsored Ad Blocker" → "Ad Blocker"; default desc updated. `popup.js`: `updateAdBlockerUI` status strings updated to reflect hard-signal scope; added comment preserving `removeSponsored` through the save handler. i18n EN+ES: updated `popup_block_sponsored/desc`, `popup_ad_blocker_on/off`, added `options_filters_removeAds` + `options_filters_removeSponsored` keys.
- **Prior (2026-04-14):** Phase 30 — Sponsored Label Detection Hardening. `_isSponsoredLabel`: added `endsWith(' token')`, `endsWith(' · token')`, `includes(' · token ·')`, `includes(' · token')` — catches FB patterns where "Sponsored" is a suffix or mid-string item (e.g. "Page · Sponsored · Follow"). `_findSponsoredMarkerInPost`: height threshold 260→400px (tall posts push label below old limit); text length limit 32→48 chars (allows "Sponsored · 3h ago" combined nodes); element scope expanded to include `[role="link"]` and all `a` elements. `_removeSponsoredByLabels`: added `aria-describedby` scan (multi-ID aware) alongside existing `aria-labelledby`. `sponsoredIndicators`: added `[role="link"][aria-label*=…]` for all 9 locales; added `adabouturl` href; added lowercase `_cft_%5b0%5d` encoding variant. `_removeAdsByHardSignals`: same `adabouturl` + lowercase `_cft_` additions.
- **Prior (2026-04-14):** Phase 29 — Image Subject Filter Hardening. All 6 image categories (sports/food/pets/vehicles/memes/travel) expanded from EN+ES to full 9-locale coverage in `selector-map.js imageSubjectTokens`. Removed `'outdoor'` and `'building'` from travel tokens (false-positive audit: food photos taken outdoors and any building photo were incorrectly triggering travel filter). Fixed alt-text prefix-stripping regex in `applyImageSubjectFilters` to handle FR (`contenir`), DE (`enthalten`), IT (`contenere`), NL (`bevatten`), SV (`innehålla`), DA (`indeholde`), NO (`inneholde`) — without this fix, non-English FB UI alt text passed the raw prefix through to token matching, causing misses. Updated regex separator to `\s*:\s*` to handle French typographic space-before-colon convention.
- **Prior (2026-04-14):** Phase 28 — Granular Sidebar Hardening. Replaced naive `document.querySelector('[role="complementary"]')` first-match with `_resolveRightSidebarContainer(rootNode)`: filters all complementary panels to those whose left edge exceeds 50% viewport width, width 100–560px, height > 200px, sorted rightmost-first — correctly targets FB's main sidebar when chat panel is also present. Expanded `_hasShortcutsHeading` from EN+ES to all 9 locales (EN/ES/FR/DE/IT/NL/SV/DA/NO). Expanded all right sidebar heading/aria-label token arrays (Trending, Contacts, Events, Birthdays) to 9 locales. Expanded `_looksLikeContactsModule` aria-label selector string, heading Set, and body-text token array to all locale variants.
- **Prior (2026-04-14):** Phase 27 — UI Tweaks Unimplemented Settings Activation. `fontSizeScale` (80–150): generates `html { font-size: N% }` when not 100; only fires when in range and non-default. `anonymizerMode`: CSS blur (8px imgs, 5px author name links) with hover-reveal transition; scoped to article/complementary/navigation. `disableCommentAutofocus`: capture-phase focus listener tracks last mousedown target; blurs programmatic textbox focus via microtask. All three were wired in options.html/js but had zero content-script implementation.
- **Prior (2026-04-14):** Phase 26 — Notification + Search Popup Full Locale Expansion. All 4 notification filter categories and all search popup detection paths expanded to FR/DE/IT/NL/SV/DA/NO.
- **Prior (2026-04-14):** Phase 25 — Memories Filter + Messenger Lifecycle Guard. `removeMemoriesPosts()`: primary href signal + multi-locale text fallback; toggle-OFF reversal in `_restoreCriticalContainers`. Messenger runtime got `_startLifecycleGuard()` + `_destroy()`.
- **Prior (2026-04-14):** Phase 24 — Content Script Lifecycle Hardening. `_startLifecycleGuard()` in main.js; `_destroy()` covers PF_Observer, PF_Cleaner, PF_NotificationControls, PF_InPageUI, PF_MessengerAI.
- **Prior (2026-04-14):** Phase 23 — Sponsored Detection Locale Completion. NL/SV/DA/NO `sponsoredIndicators` selectors; `removeRightRailAds` driven by `sponsoredTokens` array.

---

## Stability Anchors

- Known stable checkpoint: `63e715064fa88ae83dd78a74a6385860ba5ddc9f` — last fully stable pre-classification build (pre-Insight-chip, pre-Model-F)
- Rule for risky work: ship new filters behind safe-mode/master toggles first; harden selectors before enabling by default.

---

## Parity Snapshot

Status key: **DONE** = complete and stable | **DONE+** = complete but intentionally expandable | **DEFERRED** = decided not to pursue yet

| Feature Area | Status | Notes |
| --- | --- | --- |
| Story/activity post filters | DONE | All 14 subtypes wired |
| Granular sidebar module controls | DONE | Position-aware resolver + 9-locale heading tokens for all left/right modules |
| Top header micro-controls | DONE | Per-icon toggles + Notification Soul-Soother jewel styles |
| Notification popup filtering | DONE | Games, birthdays, marketplace, engagement — all 9 locales |
| Search popup suppression | DONE | All suggestions / trending / recent — all 9 locales |
| Image subject filtering | DONE | 6 categories; 9-locale alt-text tokens; false-positive audit complete |
| Auto comment preview v3.0 (shell) | DONE+ | Placeholder shell; MutationObserver guard — v3.1 real data deferred separately |
| Smart feed quality scoring | DONE | Ragebait/credibility/engagement-bait; insight chips; score thresholds; blocklist/allowlist with options manager |
| Feed mode presets | DONE | Custom/Clean/Focus/Ultra Fast/Smart/Classic |
| Performance layer | DONE | Observer queue caps; pipeline budget slicing; serialized resweeps |
| Reels limiter v2 | DONE | Session limiter + hard-lock mode |
| Power-User Rule Engine & Zap | DONE | Selector + text rules; context-menu Zap + Undo |
| Daily Feed Report | DONE | Session + weekly report; Alt+Shift+R; in-page panel |
| Right-click quick filter | DONE | Teach PureFusion context menu; blocklist/allowlist/source actions |
| Multilingual phrase packs | DONE | EN/ES/FR/DE/IT/NL/SV/DA/NO across all filter paths |
| Content script lifecycle | DONE | Port-disconnect guard; destroy() on all interval/observer modules |
| UI tweaks (font/anon/autofocus) | DONE | fontSizeScale, anonymizerMode, disableCommentAutofocus all active |
| Sponsored post detection | DONE+ | Hard-signal + label-heuristic split; iterating detection coverage |
| Auto comment preview v3.1 (real data) | DEFERRED | GraphQL interception — ToS-adjacent; needs explicit go decision |
| Plugin SDK | DEFERRED | Extension plugin hooks — low priority; after core is stable |
| Messenger enhancements | DONE | Phase 43 — always-visible timestamps, mark-all-read, conversation filter, unsend detection |
| Marketplace local filter | DONE | Phase 44 — client-side distance parsing, max-distance slider, local-first sort, MutationObserver for scroll |
| True-affinity feed sort | DONE | Phase 48 — CSS flexbox order by local engagement score; options toggle added |
| Distraction-free reading mode | DONE | Phase 48 — Alt+Shift+F shortcut + options toggle; hides sidebars, centers feed to 680px |
| Friend activity feed insight | DONE | Phase 49 — badges unseen contacts in right-rail after ≥3 authors seen in session |

---

## Remaining Work (Priority Order)

**No phases currently queued.**

### Deferred — Auto Comment Preview v3.1 — Real Comment Data

v3.0 shell injection is DONE. v3.1 would intercept FB GraphQL responses via the service worker to populate real comment text. High complexity and ToS-adjacent. **Do not start without an explicit go decision.**

### Deferred — Plugin SDK

Extension-safe plugin hooks for custom filters and post scoring. Gate behind developer mode. **Do not start until explicitly requested.**

---

## Known Risk Areas

- **Sponsored detection** still evolving — FB periodically changes the label DOM structure; `_isSponsoredLabel` may miss new variants
- **Insight classification thresholds** may need tuning — token banks are broad; word-boundary matching (Phase 40) eliminates the worst substring false positives, but semantic ambiguity ('government' in non-political contexts) remains
- **Comment preview v3.0 injection stability** — shell injection can shift under heavy scroll if FB aggressively recycles feed nodes

---

## Safety Rules

- Never hide structural containers: `html`, `body`, `[role=main]`, `[role=feed]`, `[role=banner]`, `[role=navigation]`, `[role=complementary]`.
- Keep panic recovery and recovery watchdog active at all times.
- Keep feature master toggles default OFF for new risky modules until validated.
- Prefer aria-label / role / href anchors over class-based selectors.
- Keep all AI/behavior features local-first and transparent.

## Red-Team Notes

- **Safe to ship:** Rule engine, Zap, feed modes, quality scoring, performance batching, reels limiter, diagnostics, all locale expansions.
- **Defer:** Plugin SDK, GraphQL comment interception (v3.1).
- **Never:** Deep shadow-profile inference, anything implying hidden account tracking.

## Regression Checklist Per Slice

1. Feed does not blank on first load.
2. Scroll and new node injection still work.
3. Toggle OFF restores all content affected by that toggle.
4. Toggle ON affects only the intended module/post type.
5. Messenger surface unaffected unless the feature explicitly targets it.
6. Rule Engine Zap cannot target the main feed container (Self-Zap Guard).
